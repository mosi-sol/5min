{
  "name": "event-listener",
  "version": "0.0.1",
  "description": "listening to the event's into the block chain for: save, view, make database, log, etc...",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "co": "",
    "click": "node index.js",
    "start": "http-server -a localhost -p 8000 -c-1"
  },
  "keywords": [
    "blockchain",
    "event",
    "listener"
  ],
  "author": "mosi",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.1",
    "ethers": "^5.6.9",
    "http-server": "^14.1.1"
  }
}
